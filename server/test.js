const clone = require('git-shallow-clone');

const slicedURLWithAccess = 
shallowClone(slicedURLWithAccess, '~/DevLeague/final-project/shallowRepos/', (stuff) => {
                      console.log('i think i should have my repo', stuff);
                    })